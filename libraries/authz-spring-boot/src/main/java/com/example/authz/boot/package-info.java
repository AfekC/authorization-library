/**
 * Spring Boot auto-configuration entry point: {@code @EnableConfigurationProperties},
 * bean wiring for the full authorization library, and the global enforcement filter
 * registration (architecture §12). Properties bind under the {@code authz.*} prefix.
 */
package com.example.authz.boot;
