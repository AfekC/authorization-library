/**
 * The local authorization engine: rule matching (wildcard scoring), permission
 * evaluation, and the decision matrix for USER, SERVICE, and USER_AND_SERVICE
 * authentication types (architecture §5/§6).
 */
package com.example.authz.engine;
