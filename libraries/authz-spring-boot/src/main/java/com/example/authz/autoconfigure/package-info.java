/**
 * Spring Boot auto-configuration slices: properties binding, fail-fast config
 * validation, token validation, role-permission cache synchronization, outbound
 * identity, observability, and the global enforcement filter. Each slice is an
 * {@code @AutoConfiguration} class registered in the {@code .imports} file.
 * Properties bind under the {@code authz.*} prefix.
 */
package com.example.authz.autoconfigure;
