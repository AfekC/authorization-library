/**
 * Audit event types and the default {@link com.example.authz.spi.Spi.AuditSink}
 * implementation (architecture §10.1). Per-decision events are emitted by the
 * enforcement filter and consumed by the configured sink.
 */
package com.example.authz.audit;
