# Auto-register module-wide JUnit 5 extensions discovered via META-INF/services
# (used to apply LogFlushExtension to every test without per-class annotations).
junit.jupiter.extensions.autodetection.enabled=true
